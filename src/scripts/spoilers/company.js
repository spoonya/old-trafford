import Spoiler from './spoiler';
import { DOM_ELEMENTS } from '../constants';

const spoilerCompany = new Spoiler(DOM_ELEMENTS.detailsCompany);

spoilerCompany.init();

export default spoilerCompany;
