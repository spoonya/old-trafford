import Spoiler from './spoiler';
import { DOM_ELEMENTS } from '../constants';

const spoilerDescrip = new Spoiler(DOM_ELEMENTS.detailsDescrip);

spoilerDescrip.init();

export default spoilerDescrip;
